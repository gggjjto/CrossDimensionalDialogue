---
description:
globs:
alwaysApply: true
---
hahuanya 项目开发指南

核心原则：
- 编写简洁、技术性的响应并提供准确的Python示例
- 使用函数式、声明式编程；在适当情况下优先使用纯函数
- 优先考虑迭代和模块化，避免代码重复
- 使用带有辅助动词的描述性变量名（如 is_active, has_permission）
- 目录和文件使用小写字母和下划线（如 routers/user_routes.py）
- 路由和工具函数优先使用命名导出
- 应用"接收对象，返回对象"（RORO）模式

python & FastAPI实现：
- 同步操作使用 def，异步操作使用 async def
- 为所有函数签名实现全面的类型提示
- 输入验证优先使用Pydantic模型而非原始字典
- 使用清晰的结构组织文件：路由、模型、模式、工具函数
- 统一返回数据规范
~~~python
# 良好函数结构示例
from typing import Annotated
from fastapi import FastAPI, Depends, HTTPException, status
from pydantic import BaseModel, Field

app = FastAPI()

# 请求/响应的Pydantic模型
class UserCreate(BaseModel):
    username: str = Field(..., min_length=3, max_length=50)
    email: str = Field(..., pattern=r"^[^@]+@[^@]+\.[^@]+")
    is_active: bool = True

class UserResponse(BaseModel):
    id: int
    username: str
    email: str
    is_active: bool

# 带有适当验证的路由
@app.post("/users/", response_model=UserResponse, status_code=status.HTTP_201_CREATED)
async def create_user(user_data: UserCreate) -> UserResponse:
    """
    使用验证数据创建新用户。
    
    参数:
        user_data: 经过验证的用户创建数据
        
    返回:
        UserResponse: 创建的用户数据
        
    抛出:
        HTTPException: 如果用户创建失败
    """
    # 通过Pydantic自动进行输入验证
    
    # 业务逻辑
    user = await create_user_in_db(user_data)
    
    if not user:
        raise HTTPException(
            status_code=status.HTTP_400_BAD_REQUEST,
            detail="无法创建用户"
        )
    
    return UserResponse(**user.dict())
~~~

错误处理与验证
- 在函数开头处理错误和边界情况
- 对错误条件使用提前返回
- 为前置条件和无效状态实现保护子句
- 将正常路径放在最后以提高可读性
- 避免不必要的else语句

~~~~python
from typing import Optional
from contextlib import asynccontextmanager
from fastapi import Request
import logging

# 配置日志
logging.basicConfig(level=logging.INFO)
logger = logging.getLogger(__name__)

# 自定义异常
class UserNotFoundError(Exception):
    pass

class UserCreationError(Exception):
    pass

# 错误处理中间件
@app.middleware("http")
async def error_handling_middleware(request: Request, call_next):
    try:
        response = await call_next(request)
        return response
    except UserNotFoundError:
        raise HTTPException(status_code=404, detail="用户未找到")
    except UserCreationError as e:
        logger.error(f"用户创建失败: {str(e)}")
        raise HTTPException(status_code=400, detail=str(e))
    except Exception as e:
        logger.exception(f"意外错误: {str(e)}")
        raise HTTPException(status_code=500, detail="内部服务器错误")

# 带有适当错误处理的服务函数
async def get_user_by_id(user_id: int) -> Optional[UserResponse]:
    """通过ID检索用户，包含全面的错误处理。"""
    if not user_id or user_id <= 0:
        logger.warning(f"提供了无效的用户ID: {user_id}")
        return None
    
    try:
        user = await fetch_user_from_db(user_id)
        if not user:
            raise UserNotFoundError(f"未找到ID为 {user_id} 的用户")
        
        return UserResponse(**user.dict())
    
    except DatabaseConnectionError as e:
        logger.error(f"数据库连接失败: {str(e)}")
        raise UserNotFoundError("服务暂时不可用") from e
~~~~

项目结构：
- 我们是后端工程师coding，负责backend文件下的开发，不可以访问frontend文件下的代码
- 以下是项目结构建议 app 文件夹下
~~~~ hash
app/
├── main.py                 # 入口，创建 FastAPI app
├── core/                   # 核心配置
│   ├── config.py           # 读取环境变量，配置数据库、API密钥等
│   ├── database.py         # SQLAlchemy/SQLModel 数据库连接
│   ├── security.py         # 认证/鉴权逻辑 (JWT/Session)
│   └── logging.py          # 日志配置
│
├── models/                 # 数据模型 (SQLModel/SQLAlchemy + Pydantic)
│   ├── character.py        # 角色表
│   ├── tag.py              # 标签表
│   └── embedding.py        # 向量表
│
├── schemas/                # Pydantic 校验模型 (输入输出 DTO)
│   ├── character.py
│   ├── tag.py
│   └── embedding.py
│
├── crud/                   # 数据库操作 (解耦业务逻辑)
│   ├── character.py
│   ├── tag.py
│   └── embedding.py
│
├── services/               # 业务逻辑层 (可以调用多个 crud、外部 API)
│   ├── character_service.py
│   ├── search_service.py   # 搜索服务 (模糊/embedding)
│   └── voice_service.py    # 语音服务 (调用 TTS/STT API)
│
├── api/                    # 路由
│   ├── deps.py             # 依赖注入 (DB, Auth)
│   ├── v1/                 # API 版本化
│   │   ├── character.py    # 角色路由
│   │   ├── tag.py
│   │   └── search.py
│
├── migrations/             # Alembic 数据库迁移文件
│   └── versions/
│
├── tests/                  # 测试
│   ├── test_character.py
│   ├── test_search.py
│   └── conftest.py         # pytest 配置
│
└── utils/                  # 工具类 (封装通用函数)
    ├── helpers.py
    ├── vector.py           # embedding 相关工具
    └── pagination.py
~~~~

使用技术栈：
- 后端：FastAPI + WebSocket + REST + SQLModel
- 数据库：PostgreSQL + pgvector
- 缓存：Redis
- 存储：MinIO/S3
- AI 模型：OpenAI GPT（MVP） → 自建 Llama/Mistral（扩展）
- STT/TTS：Whisper + Coqui TTS（可切换云服务） （还未确定）
- 实时语音：WebRTC + mediasoup
- 基础设施：Docker/K8s，GitHub Actions


运行python文件
- 要确保在虚拟环境中运行
~~~~bash
cd backend
./.venv/Scripts/Activate.ps1
~~~~

目前的操作系统是：windows11

我在进行团队合作使用GitHub和git， github Actions进行代码部署，使用Docker进行容器化。

不可以删除文件
不使用_init_.py
在进行 from 操作时，从 app. 开始