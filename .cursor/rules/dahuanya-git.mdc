---
alwaysApply: true
---
git 使用指南
1. 提交原子性原则
~~~bash
每个提交应该只做一件事

# ❌ 错误：一个提交包含多个不相关的更改
git commit -m "添加用户功能和修复登录bug"

# ✅ 正确：分开提交
git commit -m "feat: 添加用户注册功能"
git commit -m "fix: 修复用户登录验证问题"
~~~

2. 频繁提交原则
~~~bash
# 每天多次提交，而不是一次性提交大量更改
git add src/models/user.py
git commit -m "feat: 实现用户模型基础结构"

git add src/routes/user_routes.py
git commit -m "feat: 添加用户创建端点"

git add tests/test_users.py
git commit -m "test: 添加用户创建测试用例"
~~~
3. 描述性提交信息原则
~~~~bash
# ❌ 错误：模糊的提交信息
git commit -m "更新代码"

# ✅ 正确：清晰的提交信息
git commit -m "fix(auth): 修复JWT令牌过期时间计算错误

- 修正了令牌过期时间从小时到分钟的转换错误
- 添加了相关的单元测试
- 影响了用户登录会话持续时间"
~~~

4. 分支策略原则
~~~~bash
# 主分支保护原则
git checkout main
git pull origin main

# 功能分支原则
git checkout -b feat/user-authentication
# 在功能分支上开发...

# 定期同步主分支
git fetch origin
git merge origin/main
~~~~

5. 分支命名规范
~~~hash
# 功能开发
git checkout -b feat/user-profile

# bug修复
git checkout -b fix/login-validation

# 热修复
git checkout -b hotfix/critical-security-issue

# 文档更新
git checkout -b docs/api-reference

# 重构
git checkout -b refactor/auth-service
~~~
6. 先拉后推原则
~~~~bash
# 在推送前总是先拉取最新代码
git pull origin main --rebase
git push origin feat/user-authentication
~~~~
7. 约定式提交原则
~~~bash
# 格式: type(scope): subject
git commit -m "feat(user): 添加用户邮箱验证功能"
git commit -m "fix(auth): 修复密码重置链接过期问题"
git commit -m "docs: 更新API接口文档"
git commit -m "chore: 更新依赖包版本"
git commit -m "test: 添加用户服务单元测试"
~~~
8.提交类型规范

|  类型	|  说明	| 示例 | 
| --- | --- | --- |
|feat	|新功能	|feat: 添加支付功能|
|fix	|bug修复	|fix: 修复内存泄漏
|docs	|文档更新	|docs: 更新README
|style	|代码格式调整	|style: 格式化代码
|refactor	|代码重构	|refactor: 重构用户服务
|test	|测试相关	|test: 添加单元测试
|chore	|构建过程或辅助工具变动	|chore: 更新CI配置
